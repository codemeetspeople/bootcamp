// Описать структуру Point с полями x и y (вещественные числа)
// Написать функцию distance, которая принимает указатель на Point и
// возвращает расстояние от точки до начала координат (0, 0)
// Подсказка: расстояние = sqrt(x*x + y*y), но sqrt использовать необязательно —
// можно посчитать вручную через цикл или просто вернуть x*x + y*y (квадрат расстояния)

#include <stdio.h>

// struct Point ...

// double distance(struct Point* p) {
//     // Your code here
// }

int main() {
    // struct Point a = {3.0, 4.0};
    // struct Point b = {0.0, 0.0};

    // printf("%.2f\n", distance(&a)); // 25.00 (если возвращаем квадрат)
    // printf("%.2f\n", distance(&b)); // 0.00

    return 0;
}
