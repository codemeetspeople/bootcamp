// Описать структуру Rectangle с полями width и height (вещественные числа)
// Написать функции:
//   - area(struct Rectangle* r) — возвращает площадь
//   - perimeter(struct Rectangle* r) — возвращает периметр
//   - scale(struct Rectangle* r, double factor) — увеличивает width и height в factor раз

#include <stdio.h>

// struct Rectangle ...

int main() {
    // struct Rectangle r = {3.0, 4.0};

    // printf("%.2f\n", area(&r));      // 12.00
    // printf("%.2f\n", perimeter(&r)); // 14.00

    // scale(&r, 2.0);
    // printf("%.2f\n", area(&r));      // 48.00

    return 0;
}
